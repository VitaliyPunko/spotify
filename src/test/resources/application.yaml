spring:
  security:
    oauth2:
      client:
        registration: { } # Disable OAuth2 clients
        provider: { }
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      platform: h2
